# Font Assets

Place the bundled UI font files here.

Current CSS expects a CJK font to be available from this folder.
If you replace the placeholder font with the OTC/TTC version, keep the file path in `src/index.css` in sync.
